<div class="container-fluid mt-2">
    <section class="content">
        <div class="card">
                <div class="card-header">
                    <div class="row">
                        <div class="col-sm-12">
                            <h3 class="card-title">Directorio de clientes</h3>
                        </div>
                        <div class="col-sm-12">
                            {{>success}}
                            {{>errors}}
                        </div>
                    </div>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <table id="example1" class="table table-bordered table-striped">
                    <thead>
                    <tr>
                        <th>Empresa</th>
                        <th>Celular</th>
                        <th>Dirección</th>
                        <th>Código postal</th>
                    </tr>
                    </thead>
                    <tbody>
                        {{#each proveedores}}
                        <tr>
                            <td><a href='/ver-proveedor/{{_id}}' class="text-info">{{Empresa}}</a></td>
                            <td>{{Celular}}</td>
                            <td>{{Direccion}}</td>
                            <td>{{CodigoPostal}}</td>
                        </tr>
                        {{/each}}
                    </tbody>
                    </table>
                </div>
                <!-- /.card-body -->
                </div>
    </section>
</div>

